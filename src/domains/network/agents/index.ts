export { AGENT_DETAIL_PATH, agentDetailPath, agentsRoutes } from "./routes";

// model/, api/, queries/, components/ and the page stay internal — Managers'
// and Commercials' own list pages need only the path helper above to link
// into this workspace (FTA §4: a domain's public surface is its front door).
