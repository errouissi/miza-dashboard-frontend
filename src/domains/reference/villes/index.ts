export { VILLES_PATH, villesRoutes } from "./routes";

// The page, api/, queries/ and components/ stay internal. The app layer needs the
// route contributions and the path (for the nav entry) — nothing else (FTA §4).
