# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment.
# Committed (public values only, localhost): .env.example, .env.development, .env.test
# Never committed: a bare .env, local overrides, and the deploy-supplied
# staging/production values. Secrets never belong in frontend config (FTA §14).
.env
.env.local
.env.*.local
.env.staging
.env.production

# Coverage / test artifacts
coverage
